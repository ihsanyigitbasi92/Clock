export const scheduleNotification = (timeZone, time) => {
  // Logic to schedule notification
  console.log(`Notification scheduled for ${timeZone} at ${time}`);
};

export const cancelNotification = (timeZone) => {
  // Logic to cancel notification
  console.log(`Notification cancelled for ${timeZone}`);
};